// 방명록에서 이상한 악성 글 삭제할 때 사용하는 코드

/**
guestbook = localStorage.guestbookEntries;
value = JSON.parse(guestbook);
value.splice(삭제할 인덱스, 1);
localStorage.setItem('guestbookEntries', JSON.stringify(value));
*/

